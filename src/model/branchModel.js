export default class branchModel {
    constructor(id, code, name, description, parentId) {
        this.id = id;
        this.code = code;
        this.name = name;
        this.description = description;
        this.parentId = parentId;
    }
}